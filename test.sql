-- FILEPATH: /Users/apple/Desktop/test/Structured/test.sql

-- Create the users table
-- CREATE TABLE IF NOT EXISTS users (
--     id INTEGER PRIMARY KEY AUTOINCREMENT,
--     username TEXT NOT NULL,
--     email TEXT NOT NULL,
--     hashed_password TEXT NOT NULL
-- );

-- Insert data into the users table
-- INSERT INTO users (username, email, hashed_password) VALUES ('jijonj', 'jijonj@gamil.com', '123notreallyhashed');
select * from users;